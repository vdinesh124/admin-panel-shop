## Packages
recharts | Dashboard analytics charts
framer-motion | Smooth animations for page transitions and interactions
lucide-react | Icons for the sidebar and UI elements (already in base, but emphasizing usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Authentication is handled via Replit Auth (use-auth.ts).
Dashboard needs a revenue chart using Recharts.
Products table needs sorting and filtering.
Orders need status update functionality.
Numeric inputs must be coerced using z.coerce.number().
